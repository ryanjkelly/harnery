---
title: harn qa-run
description: Run the whole page-QA matrix (planner, deterministic gates, interactions, critique, snapshot) in one command with a fail-closed result.
sidebar:
  order: 25
---

```
harn qa-run [options] <target>
```

Executes a complete page-QA matrix against one target (URL, local file path, or framework route) in a single invocation. Without it, a signoff means a long serial sequence of [`harn browse`](/cli/browse/) commands, one per viewport, theme, state, interaction, and critique pass. `qa-run` runs that whole sequence itself: the QA planner first, then the deterministic gates for every required context through a bounded child-process pool, then interaction assertions serially, then the manifest-required vision critique one context at a time, and finally the QA snapshot. It writes `page-qa-result.json` with a terminal verdict of `passed`, `failed`, or `incomplete`.

Every child is a real `browse` invocation of the same host CLI (argv arrays only, never a shell string), so profile, cookie, and critique-provider semantics are byte-identical to running the commands by hand.

## Options

| Flag | Description |
| --- | --- |
| `--job <file>` | QA job document (JSON, see below). Validated before any browser starts; all validation errors print at once. Absent: a minimal implicit job for `<target>`. |
| `--mode <mode>` | `signoff` persists a QA snapshot on a passing run; `review` never does. Default `signoff`. An explicit `--mode` overrides the job file's mode. |
| `--concurrency <n>` | Concurrent deterministic captures (1–8; default 2). Interactions and critique always run serially. |
| `--allow-metered` | Permit the critique provider's metered-API fallback. Default: subscription-backed headless harnesses only. |
| `--out-dir <dir>` | Artifact + result directory. Default `.qa-run/<timestamp>` under the current directory. |
| `--json` | Print the full result JSON to stdout (the file is written either way). |

## Exit codes

| Code | Meaning |
| --- | --- |
| 0 | Verdict `passed`. |
| 1 | Usage error or job validation failure (nothing was executed). |
| 2 | Verdict `failed`: a completed check found a defect. |
| 4 | Verdict `incomplete`: something could not be established (blockers name it). |

## Execution order

1. **Plan.** `browse --qa-plan` classifies the render against the persisted QA baseline and emits the coverage manifest. A missing, unparseable, or incomplete manifest is a `plan` blocker: the run stops before any further browser work and the verdict is `incomplete`.
2. **Deterministic gates.** Coverage is the union of the manifest's contexts and the job's extra contexts; a job can widen the matrix but never narrow it below the manifest. Each context (viewport × theme × state) gets one child `browse` invocation carrying all of its applicable job checks, with a unique `--out` prefix under the run directory. Dark contexts render through `browse --color-scheme dark`. Exit 0 is a pass; exit 2 is a failed gate (failure details are parsed from the JSON artifact); anything else (a timeout, a spawn error, a missing artifact) is an `unknown` outcome plus a named blocker.
3. **Interactions.** Each `interaction_states` entry runs serially: its setup flags, then every assertion as `--assert` with `--assert-fail`. A manifest-required interaction state the job does not declare is a blocker: coverage may never be narrower than the manifest.
4. **Critique.** Only when every gate and interaction passed and the manifest requires a visual pass. One context at a time (the critique provider owns tile concurrency), with `--check-critique-fail`, `--qa-reuse` when a baseline exists, and, in signoff mode, `--qa-snapshot` riding in the same invocation. Unless the job permits metered critique, children run with `HARNERY_CRITIQUE_HEADLESS_ONLY=1`; an exhausted headless list becomes an `incomplete` blocker naming the policy, never a metered call.
5. **Snapshot.** Signoff runs whose manifest required no visual pass still persist their baseline through a dedicated snapshot pass. A signoff verdict is only `passed` when every context's snapshot was saved.
6. **Result.** `page-qa-result.json` in manifest order regardless of completion order: the manifest, merged contexts, every command's argv/exit/outcome/failures/artifacts/wall-time, critique outcomes, snapshot record, per-stage wall times, blockers, and the verdict.

## The job document

A job (`--job`) widens or parameterizes the run. It is validated before anything executes; structural errors and secret-shaped fields are all reported at once.

```json
{
  "schema_version": 1,
  "target": "http://localhost:3000/pricing",
  "mode": "signoff",
  "contexts": [
    { "id": "hd-dark-default", "viewport": "hd", "theme": "dark", "state": "default" }
  ],
  "checks": [
    { "id": "overflow", "args": ["--check-overflow", "--check-overflow-fail"] },
    { "id": "hero-contrast", "args": ["--check-contrast", ".hero", "--check-contrast-fail"], "contexts": ["desktop-light-default"] }
  ],
  "interaction_states": [
    { "name": "menu-open", "setup": ["--click", "#menu"], "assertions": ["exists .menu-panel"] }
  ],
  "qa_hints": { "scopes": ["#main"], "states": ["menu-open"] },
  "policy": { "command_concurrency": 2, "command_timeout_ms": 120000 }
}
```

- The command's `<target>` argument is authoritative; a job file may omit `target`.
- `contexts` append to the planner's matrix (union, never narrowing). Derived context IDs are `<viewport>-<theme>-<state>`.
- `checks[].args` are argument arrays appended to the context's capture command, never shell strings. A check without `contexts` applies to every context. The runner always adds the planner's deterministic checks. Job checks can add coverage but cannot omit the manifest floor.
- Each interaction needs at least one outcome assertion: a click without a proven outcome is not a gate.
- **Jobs must not carry credentials.** Secret-bearing field names (`token`, `password`, `cookie`, …) and credential-shaped values (`Bearer …`) are refused; authentication is referenced through existing browser profiles and cookie stores.

## Verdict rules (fail-closed)

`passed` requires an intact planner manifest, every gate and interaction passed, every required critique tile with a known outcome, and, in signoff mode, the snapshot persisted. A defect found by a completed check is `failed`. Anything the runner could not establish (a blocker, an unknown outcome, a dead child, a missing artifact) is `incomplete`; an incomplete run is never a pass.

## Examples

```bash
# Review a local route with the implicit minimal job
harn qa-run http://localhost:3000/pricing --mode review

# Full signoff from a job file, results as JSON
harn qa-run http://localhost:3000/pricing --job pricing-qa.json --json

# Wider pool, metered critique fallback permitted
harn qa-run https://example.com/landing --concurrency 4 --allow-metered
```

## See also

- [`harn browse`](/cli/browse/): the command each child runs; owns every check flag, `--qa-plan`, `--qa-snapshot`, and `--color-scheme`.
