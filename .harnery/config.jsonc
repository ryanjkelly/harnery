{
  // This repo IS harnery — agents working on it use the repo-local `bin/harn`.
  // Pinned so a host-embedded checkout can't re-stamp a host bin name into the
  // committed AGENTS.md block or skills (they're public, generic surfaces).
  "binName": "harn"
}
