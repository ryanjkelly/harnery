/**
 * The mark rendered in a table cell, chip, or label when a value is missing
 * (null, undefined, or empty). Defined once so the dashboard shows one
 * consistent "no data" glyph instead of scattered literals.
 */
export const NO_DATA = "—";
