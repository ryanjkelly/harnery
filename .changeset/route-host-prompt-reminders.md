---
"harnery": minor
---

Add a project-owned `instructions.promptReminder` that Harnery injects on every
supported prompt without duplicating native adapter reminders or persisting the
host text in coordination state.
