---
"harnery": minor
---

Derive backup snapshots from the storage catalog, enforce a configurable size limit, add per-host freshness throttling, and allow SessionStart to launch scheduled snapshots without delaying the agent.
