---
"harnery": minor
---

Add phased CLI commands for proof-gated workspace integration and conservative
cleanup, with explicit policy, review, and mutation confirmation. When
integration prepare parks on a host-policy ASK, expose the exact run, plan, and
approval IDs as a handled park instead of a generic failure.
