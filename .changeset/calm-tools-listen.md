---
"harnery": patch
---

Accept one unambiguous schema-gated JSON object or array after a harness-added leading sentence while continuing to reject trailing prose, multiple candidates, malformed values, and schema mismatches.
