---
"harnery": patch
---

Name `agents lifecycle active` in every refusal a continuing session hits after its generation ended: `whoami`, `status`, `set-task`, `identity assume`, and `heal` now point at the one command that opens a fresh generation instead of leaving the caller to guess.
