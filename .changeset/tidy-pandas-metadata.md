---
"harnery": patch
---

Add the `harnery-doc/v2` markdown metadata validator and `docs metadata validate` audit command. The audit reports valid, invalid, legacy, and missing files and provides a strict cutover mode for hosts migrating their documentation corpus.
