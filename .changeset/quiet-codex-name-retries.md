---
"harnery": patch
---

Bound Codex session-name recovery when assistant transcript evidence is unavailable. Routine `agents set-task` calls stay title-silent, pending prompt reminders emit once per minted name, and `agents suggest-name --json` creates the explicit retry boundary with the exact stored name.
