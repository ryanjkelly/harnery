---
"harnery": patch
---

Re-attach the session-name display instruction as PostToolUse context on up to two ordinary tool results while the requested title stays pending. The fail-open gate from ADR 0182 stays; the reminder gives a model that skipped the block a second chance without a denial the transcript cannot support.
