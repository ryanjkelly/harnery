---
"harnery": minor
---

Add the product-tier `harnery/core/semantic` contract, deterministic Event Ledger V3 evidence projection, exact harness-routed readers, typed receipts, bounded scheduling, atomic derived storage, and the `harn semantic` inspection and one-shot CLI.
