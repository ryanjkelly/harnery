---
"harnery": patch
---

Retry page-review gates and allocation when a lazy image changes document geometry during an incomplete native capture. Stable out-of-bounds captures still fail, and every tile must retain native coverage.
