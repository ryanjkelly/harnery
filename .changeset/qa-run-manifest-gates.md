---
"harnery": patch
---

Make `qa-run` execute every deterministic check in the planner manifest, fail on recorded browser diagnostics when `console` is required, and stop incomplete when a future planner check has no executable mapping.
