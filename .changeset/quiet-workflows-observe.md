---
"harnery": minor
---

Add opt-in shadow admission telemetry to workflow and governor runs. `--observe-pressure` demand-starts the optional supervisor only before a real child dispatch, waits for a fresh cycle, records bounded diagnostic advice in the transcript, proof, and run report, and always leaves dispatch unchanged.
