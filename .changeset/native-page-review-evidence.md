---
"harnery": minor
---

Keep native tiles as the evidence for page review and snapshot reuse. Version 2 packs store a half-size overview, bind reviewer decisions to exact native pixels and finding text, and require recapture of older packs. QA runs allocate one tile budget across all contexts, report gaps explicitly, and retain capture evidence when a deterministic gate fails. Reviewed decisions survive pack expiry and can affect later signoffs only when explicitly enabled.
