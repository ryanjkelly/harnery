---
"harnery": patch
---

The AGENTS.md orientation block now teaches the `agents lifecycle` command: declaring `blocked`, `done`, and `active`, the Git finalization gate on `done`, and that ordinary `set-task` calls never change lifecycle. Consumers pick the paragraph up on their next `init` re-splice.
