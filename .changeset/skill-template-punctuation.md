---
"harnery": patch
---

Normalize punctuation in the shipped `harn-decide` and `harn-council` skill templates (and the owned-skill header marker). Instruction prose only; decision and council engine behavior is unchanged.
