---
"harnery": patch
---

Skip hard-expired live-display generation files on read and sweep them from the supervisor every five minutes. A directory of stale generation files no longer costs the dashboard a full parse on every request.
