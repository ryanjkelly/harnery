---
"harnery": patch
---

Allow `harn agents heal --kind heartbeat` to accept `--adapter`, so external restart tooling can recreate a missing Codex or Cursor heartbeat without mislabeling it as Claude Code.
