---
"harnery": minor
---

Add rendered alignment, gap, clipping, target-size, and sibling-overlap checks to `browse`, with structured outcomes, screenshot annotations, and fail gates that run together in one browser state.
