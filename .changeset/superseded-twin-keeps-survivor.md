---
"harnery": patch
---

When a later generation for the same instance is superseded, the safety
projection retargets current generation to the surviving live sibling so
whoami and set-task still see the live session.
