/** First-party harness adapters supported by Harnery's canonical V2 ledger. */
export type Adapter = "claude-code" | "cursor" | "codex";

/** Canonical producer classes used by V2 provenance. */
export type EventSource = "agent-hooks" | "agent-coord" | "user" | "system";
